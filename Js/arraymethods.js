//translate

        // let str=prompt("enter a string");
        // arr=str.split('-');
        // arr1=arr.map((word, index) => index == 0 ? word : word[0].toUpperCase() + word.slice(1));
        // arr2=arr1.join('');
        // alert(arr2);

        //filterrange

        //     let filter=(arr,max,min)=>{
        //     let arr1=[];
        //     arr1=arr.filter(item => (a <= item && item <= b));
        //         arr1.push(item);
        //     }

        //  let arr=[];
        //  while(item=+prompt("enter a number"))
        //  {
        //     arr=arr.push(item);
        //  }
        // max=prompt("enter maximum");
        // min=prompt("enter min");
        // reqArr=filter(arr,max,min);

